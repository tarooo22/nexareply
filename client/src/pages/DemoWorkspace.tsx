import { ThemeSelector } from "@/components/ThemeSelector";
import { demoConversations } from "@/lib/demo-data";
import { BarChart3, Bell, BookOpenText, Bot, ChevronDown, LayoutDashboard, Menu, MessageSquareText, PackageSearch, Settings2, ShieldCheck, UsersRound, X } from "lucide-react";
import { lazy, Suspense, useMemo, useState } from "react";
import { Link, useLocation } from "wouter";
import ConversationsView from "./demo/ConversationsView";
import KnowledgeView from "./demo/KnowledgeView";
import LeadsView from "./demo/LeadsView";
import NotificationsView from "./demo/NotificationsView";
import ProductsView from "./demo/ProductsView";
import SettingsView from "./demo/SettingsView";

type DemoSection = "overview" | "conversations" | "products" | "knowledge" | "leads" | "analytics" | "settings" | "notifications";
const AnalyticsView = lazy(() => import("./demo/AnalyticsView"));
const navItems: Array<{ section: DemoSection; label: string; icon: typeof LayoutDashboard }> = [
  { section: "overview", label: "მიმოხილვა", icon: LayoutDashboard }, { section: "conversations", label: "საუბრები", icon: MessageSquareText }, { section: "products", label: "სუნამოები", icon: PackageSearch }, { section: "knowledge", label: "ცოდნის ბაზა", icon: BookOpenText }, { section: "leads", label: "ლიდები და შეკვეთები", icon: UsersRound }, { section: "analytics", label: "ანალიტიკა", icon: BarChart3 }, { section: "notifications", label: "შეტყობინებები", icon: Bell }, { section: "settings", label: "პარამეტრები", icon: Settings2 },
];

function currentSection(location: string): DemoSection { const candidate = location.replace(/^\/demo\/?/, "").split("/")[0] || "overview"; return navItems.some((item) => item.section === candidate) ? candidate as DemoSection : "overview"; }
function Metric({ label, value, hint }: { label: string; value: string | number; hint: string }) { return <article className="rounded-2xl border border-border bg-card p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-[.12em] text-muted-foreground">{label}</p><p className="mt-3 text-3xl font-black text-primary">{value}</p><p className="mt-2 text-xs leading-5 text-muted-foreground">{hint}</p></article>; }
function Overview() { const priority = demoConversations.filter((item) => item.ticket || item.priority === "high").slice(0, 3); return <div className="space-y-5"><section className="rounded-3xl border border-border bg-card p-6 shadow-sm"><p className="text-sm font-bold text-primary">Amadeo perfume demo</p><h1 className="mt-1 text-2xl font-black md:text-3xl">სადემონსტრაციო გაყიდვების workspace</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-muted-foreground">ეს არის უსაფრთხო public demo — ყველა პროდუქტი, policy და საუბარი არის დემო მონაცემი. რეალური Amadeo მონაცემები და Messenger Page ხელმისაწვდომია მხოლოდ დაცული workspace-იდან.</p><Link href="/demo/conversations" className="mt-5 inline-flex min-h-11 items-center gap-2 rounded-xl bg-primary px-4 text-sm font-bold text-primary-foreground">გახსენით Demo Inbox <MessageSquareText className="size-4" /></Link></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="დემო საუბრები" value={demoConversations.length} hint="სუნამოების ინტერესის მაგალითები" /><Metric label="human handoff" value={demoConversations.filter((item) => item.ticket || item.humanActive).length} hint="უცნობი კითხვა ან takeover" /><Metric label="ღია ინტერესები" value={demoConversations.filter((item) => item.status === "open").length} hint="რეალური flow-ის ვიზუალური მაგალითი" /><Metric label="უსაფრთხოების წესი" value="catalog" hint="AI არ იგონებს პირობებს" /></section><section className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><article className="rounded-2xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><p className="text-sm font-bold text-primary">სწრაფი რეაგირება</p><h2 className="mt-1 text-xl font-black">პრიორიტეტული საუბრები</h2></div><Link href="/demo/conversations" className="text-sm font-bold text-primary">Inbox</Link></div><div className="mt-5 space-y-3">{priority.map((item) => <Link key={item.id} href={`/demo/conversations?conversation=${item.id}`} className="flex items-center justify-between gap-3 rounded-xl border border-border p-4 hover:bg-secondary/50"><div className="min-w-0"><p className="font-bold">{item.customer}</p><p className="mt-1 truncate text-sm text-muted-foreground">{item.preview}</p></div><span className={`rounded-full px-2 py-1 text-xs font-bold ${item.ticket ? "bg-rose-100 text-rose-800" : "bg-amber-100 text-amber-800"}`}>{item.ticket ? "ticket" : "ლიდი"}</span></Link>)}</div></article><article className="rounded-2xl border border-border bg-card p-6"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-primary/10 text-primary"><ShieldCheck className="size-5" /></span><div><p className="text-sm font-bold text-primary">Demo safety</p><h2 className="mt-1 font-black">provider secrets არ ჩანს</h2></div></div><p className="mt-5 text-sm leading-6 text-muted-foreground">Demo არ უკავშირდება რეალურ Meta Page-ს, access token-ს ან მომხმარებელთა ნამდვილ მონაცემებს. წარმოების Amadeo Workspace იყენებს persistent tenant-scoped მონაცემებს.</p><Link href="/app" className="mt-5 inline-flex min-h-10 items-center text-sm font-bold text-primary">დაცულ workspace-ზე გადასვლა</Link></article></section></div>; }

export default function DemoWorkspace() {
  const [location, setLocation] = useLocation(); const [navOpen, setNavOpen] = useState(false); const [role, setRole] = useState<"owner" | "operator">("owner"); const [debounceSeconds, setDebounceSeconds] = useState(10); const section = useMemo(() => currentSection(location), [location]); const visible = navItems.filter((item) => role === "owner" || item.section !== "settings"); const navigate = (next: DemoSection) => { setNavOpen(false); setLocation(next === "overview" ? "/demo" : `/demo/${next}`); };
  return <div className="min-h-screen bg-background text-foreground lg:flex">{navOpen ? <button onClick={() => setNavOpen(false)} className="fixed inset-0 z-40 bg-black/30 lg:hidden" aria-label="ნავიგაციის დახურვა" /> : null}<aside className={`fixed inset-y-0 left-0 z-50 flex w-72 flex-col border-r border-sidebar-border bg-sidebar p-4 transition-transform lg:sticky lg:top-0 lg:h-screen lg:translate-x-0 ${navOpen ? "translate-x-0" : "-translate-x-full"}`}><div className="flex items-center justify-between border-b border-sidebar-border pb-4"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-primary font-black text-primary-foreground">A</span><div><p className="font-black">Amadeo Demo</p><p className="text-xs text-muted-foreground">perfume sales flow</p></div></div><button onClick={() => setNavOpen(false)} className="rounded-lg p-2 lg:hidden"><X className="size-4" /></button></div><div className="mt-5 rounded-xl border border-sidebar-border bg-sidebar-accent/50 p-3"><div className="flex items-center justify-between"><span className="text-sm font-bold">Demo როლი</span><ChevronDown className="size-4 text-muted-foreground" /></div><div className="mt-3 grid grid-cols-2 gap-2"><button onClick={() => { setRole("owner"); }} className={`min-h-9 rounded-lg text-xs font-bold ${role === "owner" ? "bg-primary text-primary-foreground" : "bg-secondary"}`}>მფლობელი</button><button onClick={() => { setRole("operator"); if (section === "settings") navigate("overview"); }} className={`min-h-9 rounded-lg text-xs font-bold ${role === "operator" ? "bg-primary text-primary-foreground" : "bg-secondary"}`}>ოპერატორი</button></div></div><nav className="mt-5 flex-1 space-y-1">{visible.map((item) => { const Icon = item.icon; return <button key={item.section} onClick={() => navigate(item.section)} className={`flex min-h-11 w-full items-center gap-3 rounded-xl px-3 text-left text-sm font-bold ${section === item.section ? "bg-primary text-primary-foreground" : "text-sidebar-foreground/80 hover:bg-sidebar-accent"}`}><Icon className="size-4" />{item.label}</button>; })}</nav><Link href="/" className="mt-4 inline-flex min-h-10 items-center rounded-xl border border-sidebar-border px-3 text-sm font-bold">NexaReply გვერდზე</Link></aside><section className="min-w-0 flex-1"><header className="sticky top-0 z-30 flex h-[68px] items-center justify-between border-b border-border bg-background/90 px-4 backdrop-blur-xl md:px-7"><div className="flex items-center gap-3"><button onClick={() => setNavOpen(true)} className="rounded-xl border border-border bg-card p-2 lg:hidden"><Menu className="size-5" /></button><div><p className="font-black">{navItems.find((item) => item.section === section)?.label}</p><p className="text-xs text-muted-foreground">Amadeo Demo · შესვლის გარეშე</p></div></div><ThemeSelector /></header><main className="p-4 md:p-7">{section === "overview" ? <Overview /> : null}{section === "conversations" ? <ConversationsView debounceSeconds={debounceSeconds} /> : null}{section === "products" ? <ProductsView /> : null}{section === "knowledge" ? <KnowledgeView /> : null}{section === "leads" ? <LeadsView /> : null}{section === "analytics" ? <Suspense fallback={<div className="rounded-2xl border border-border bg-card p-8 text-sm">ანალიტიკა იტვირთება…</div>}><AnalyticsView /></Suspense> : null}{section === "notifications" ? <NotificationsView /> : null}{section === "settings" && role === "owner" ? <SettingsView debounceSeconds={debounceSeconds} onDebounceChange={setDebounceSeconds} /> : null}</main></section></div>;
}
